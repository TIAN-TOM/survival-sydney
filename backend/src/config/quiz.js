// Subsystem D - Integration, Robustness & Documentation (Tom Tian):
// shared quiz constants used by controllers, models, and tests.
module.exports = {
  QUIZ_LENGTH: 10,
  OPTIONS_PER_QUESTION: 4,
  ATTEMPT_TOKEN_TTL_SECONDS: 2 * 60 * 60,
};
