# Architecture

This document has moved to the repository root: see [ARCHITECTURE.md](../ARCHITECTURE.md).

The root document covers the full system layout of Survival Sydney, the exam-integrity
engine (signed attempt tokens, per-question atomic locking, server-side scoring and
replay protection), the data model, and the decision log.
