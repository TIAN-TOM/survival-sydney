/** Homepage start hero — wide art (quiz start screen only). */
export function quizStartHeroImageSrc(isDarkMode) {
  return isDarkMode ? '/logo-bg-night.png' : '/logo-bg-day.png';
}

/** Compact seal / nav logo. */
export function quizSealLogoSrc(isDarkMode) {
  return isDarkMode ? '/logo-night.png' : '/logo-day.png';
}
